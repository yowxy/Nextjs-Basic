export default function layout ({children}){
    return(
        <html>
            <head>
                <title>halo</title>
            </head>
            <body>{children}</body>
        </html>
    );
}